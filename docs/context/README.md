# Context Packs

Context Packs temporários ou reutilizáveis podem ser registrados aqui quando uma tarefa cross-domain exigir seleção além das rotas de `context-map.yaml`. O diretório não substitui os artefatos autoritativos.
