# Features

Cada feature relevante criada sob a governança v4 deve usar `docs/features/FEATURE-ID/` com `feature-prd.md`, `feature-spec.md`, `context.yaml` e `status.md`. Features históricas continuam rastreáveis pelos documentos existentes, ADRs e release records; serão migradas somente quando voltarem ao escopo ativo.
